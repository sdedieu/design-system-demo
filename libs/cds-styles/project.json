{
  "name": "cds-styles",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/cds-styles/src",
  "prefix": "ds-cds",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx sass --load-path=node_modules libs/cds-styles/src:dist/cds-styles --no-source-map"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
