# Design System Demo

[Nx](https://nx.dev/) workspace about desing system on top of Angular Material playground.

## Installing dependencies

Run `npm ci` to install node dependencies.

## Build cds-libraries

Run `npx nx build cds-library && npx nx build cds-styles`.

## Run projects

Run `npm run start:all`.
